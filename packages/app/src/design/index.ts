/**
 * Maay Design System
 * 
 * Centralized design tokens and reusable styles for consistent UI.
 * 
 * Usage:
 * ```ts
 * import { colors, fonts, spacing, getThemeColors } from '@/design';
 * import { getPrimaryButtonStyle, getSheetStyle } from '@/design';
 * ```
 */

// Export all tokens
export * from './tokens';

// Export all styles
export * from './styles';
